import 'package:flutter/material.dart';

class PageCharacterStories extends StatelessWidget {
  const PageCharacterStories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina Characteres'),
    );
  }
}
