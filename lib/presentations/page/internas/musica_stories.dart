import 'package:flutter/material.dart';

class PageMusicStories extends StatelessWidget {
  const PageMusicStories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina Musica'),
    );
  }
}
